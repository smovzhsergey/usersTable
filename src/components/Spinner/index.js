// Core
import React from 'react';

// Instruments
import Styles from './styles';

const Spinner = () =>
    <section className = { Styles.spinnerCont } >
        <span className = { Styles.spinner } />
    </section>;

export default Spinner;
